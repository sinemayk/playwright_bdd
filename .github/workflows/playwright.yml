name: Playwright Tests
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      
      - name: Run Playwright tests
        run: npx playwright test
        env:
          OPENSOURCE_URL: ${{ secrets.OPENSOURCE_URL }}
          OPENSOURCE_USERNAME: ${{ secrets.OPENSOURCE_USERNAME }}
          OPENSOURCE_PASSWORD: ${{ secrets.OPENSOURCE_PASSWORD }}
          SAUCE_DEMO_USERNAME_STANDARD: ${{ secrets.SAUCE_DEMO_USERNAME_STANDARD }}
          SAUCE_DEMO_USERNAME_ERROR: ${{ secrets.SAUCE_DEMO_USERNAME_ERROR }}
          SAUCE_DEMO_USERNAME_PROBLEM: ${{ secrets.SAUCE_DEMO_USERNAME_PROBLEM }}
          SAUCE_DEMO_PASSWORD: ${{ secrets.SAUCE_DEMO_PASSWORD }}
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      - name: Get Allure history
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages
      - name: gh-pages klasörünün var olduğundan emin ol
        if: always()
        run: mkdir -p gh-pages

      - name: Generate Allure report
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: allure-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history
      - name: Deploy report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history
      - name: n8n'e bildirim gönder
        if: always()
        run: |
          curl -X POST "${{ secrets.N8N_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d "{\"repo\": \"${{ github.repository }}\", \"status\": \"${{ job.status }}\"}"